var x = 10;

function sum(a,b) { 
   return a + b;
 }

/**
 * Adds two numbers
 * @param {String} x 
 * @return {String}
 */
function hello(x) {
  return "hello" + x;
}
var x = sum(1,1);
var y = hello();
var bar = foo.slice(0, 2).join("").split("a")[0];
var m = /.+\.([^.]+)$/.exec(val);
bar
hello
hello
ddddh
hasOwnProperty
